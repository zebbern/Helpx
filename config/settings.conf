#!/bin/bash

# Active user (determined in main script)
# ACTIVE_USER should be exported by main.sh if needed across modules
export ACTIVE_USER="${ACTIVE_USER:-$(whoami)}"

# Shell RC file
export RC_FILE="/home/$ACTIVE_USER/.bashrc" # or .zshrc based on detection